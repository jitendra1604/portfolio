import { buildingApps, type BuildingApp } from "@/data/apps";

const typeLabels: Record<BuildingApp["type"], string> = { web: "Web app", flutter: "Flutter app", extension: "Browser extension", cli: "CLI tool" };
const actionLabels: Record<BuildingApp["type"], string> = { web: "Try it", flutter: "Get it on Play Store", extension: "Add to Chrome", cli: "View on GitHub" };

export default function BuildingInPublicSection() {
  return <section id="building-in-public" className="bg-background px-6 py-24 text-ink md:py-32"><div className="mx-auto max-w-[1200px]"><p className="text-xs uppercase tracking-[0.3em] text-caption">Building in public</p><h2 className="mt-3 text-4xl font-bold tracking-tight md:text-5xl">What I’m building now</h2><div className="mt-10 grid gap-6 md:grid-cols-2">{buildingApps.map((app) => <article key={app.name} className="card"><div className="flex aspect-video items-center justify-center rounded-lg border border-dashed border-line-strong bg-black/20 text-sm text-caption">{app.previewSrc ? <video autoPlay muted loop playsInline className="h-full w-full rounded-lg object-cover" aria-label={`${app.name} product preview`}><source src={app.previewSrc} /></video> : "Demo preview coming soon"}</div><div className="mt-5 flex items-center justify-between gap-4"><span className="text-xs uppercase tracking-[0.18em] text-caption">⌘ {typeLabels[app.type]}</span><span className="inline-flex items-center gap-2 text-xs text-body"><span className={`h-2 w-2 rounded-full ${app.status === "live" ? "bg-accent" : app.status === "testing" ? "bg-amber-300" : "bg-sky-300"}`} />{app.status}</span></div><h3 className="mt-4 text-2xl font-semibold">{app.name}</h3><p className="mt-2 text-body">{app.pitch}</p>{app.href && <a href={app.href} className="mt-5 inline-flex text-sm font-medium text-accent hover:text-ink">{actionLabels[app.type]} <span aria-hidden="true">→</span></a>}</article>)}</div>{buildingApps.length === 0 && <p className="mt-8 rounded-xl border border-dashed border-line-strong p-6 text-body">Add your first app in <code>src/data/apps.ts</code>; cards, status badges, previews, and type-specific calls to action are generated automatically.</p>}</div></section>;
}
